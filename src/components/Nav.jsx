const Nav = () => {
  const styles = {
    width: "100%",
    height: "70px",
    backgroundColor: "black",
    color: "white",
  };
  return (
    <div>
      <nav style={styles}>Nav</nav>
    </div>
  );
};

export default Nav;
